appId: com.ghostly.app
productName: Ghostly
npmRebuild: false
afterPack: build/after-pack.cjs
directories:
  buildResources: build
  output: dist
publish:
  provider: github
  owner: nipun-mohan
  repo: Ghostly-AI
  releaseType: release
files:
  - "out/**"
  - "!**/.vscode/*"
  - "!src/*"
  - "!electron/*"
  - "!{.eslintignore,.eslintrc.cjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}"
  - "!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}"
asarUnpack:
  - resources/**
win:
  executableName: Ghostly
  target:
    - target: nsis
      arch:
        - x64
nsis:
  artifactName: ${name}-${version}-setup.${ext}
  shortcutName: Ghostly
  uninstallDisplayName: Ghostly
  createDesktopShortcut: always
mac:
  category: public.app-category.productivity
  icon: icon.png
  identity: "-"
  hardenedRuntime: true
  gatekeeperAssess: false
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  extendInfo:
    NSCameraUsageDescription: "Ghostly uses the camera only when a feature explicitly requests it."
    NSMicrophoneUsageDescription: "Ghostly uses the microphone to transcribe interview audio in real time."
    NSAudioCaptureUsageDescription: "Ghostly captures system audio so interview questions can be transcribed in real time."
    NSScreenCaptureUsageDescription: "Ghostly captures the screen when you request AI screen analysis."
    NSDocumentsFolderUsageDescription: "Ghostly accesses documents only when you select them."
    NSDownloadsFolderUsageDescription: "Ghostly accesses downloads only when you select them."
  notarize: false
  target:
    - target: dmg
      arch:
        - x64
        - arm64
    - target: zip
      arch:
        - x64
        - arm64
dmg:
  artifactName: ${name}-${version}-${arch}.${ext}
linux:
  target:
    - target: AppImage
      arch:
        - x64
  maintainer: ghostly
  category: Utility
appImage:
  artifactName: ${name}-${version}.${ext}
